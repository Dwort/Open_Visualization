import React, {useState} from "react";

const PremiumStatus = ({userId}) => {
    const [userPremium, serUserPremium] = useState(null);


  return (
      <div>

      </div>
  );
};

export default PremiumStatus;